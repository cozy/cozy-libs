@require 'components/forms.styl'

.shared-drive-content-wrapper
  display: flex
  flex-direction: column
  align-items: center
  gap: 1rem

.shared-drive-who-has-access-wrapper
  border: 1px solid var(--borderMainColor)
  border-radius: 0.5rem
  width: 100%
  padding: 1rem
  box-sizing: border-box

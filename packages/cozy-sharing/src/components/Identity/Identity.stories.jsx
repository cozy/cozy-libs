import Identity from './Identity'

const meta = {
  title: 'Components/Identity/Identity',
  component: Identity,
  args: {}
}

export default meta

export const Default = {
  name: 'Default',
  args: {
    name: 'John Doe'
  }
}

import { ConnectionBackdrop } from './ConnectionBackdrop'

const meta = {
  component: ConnectionBackdrop,
  arg: {
    name: 'EDF'
  }
}

export default meta

export const Default = {
  name: 'Default'
}
